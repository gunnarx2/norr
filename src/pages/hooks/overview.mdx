import { Callout, Steps, Tabs } from 'nextra/components';

# Overview

<Callout type="error" emoji="🚫">
  `@norr/hooks` is in a early stage and is subject to change.
</Callout>

Collection of [React hooks](https://react.dev/reference/react/hooks).

## Install

<Steps>
  ### Package

  <Tabs items={['yarn', 'npm']}>
    <Tabs.Tab>
      ```bash copy
      $ yarn add @norr/hooks
      ```
    </Tabs.Tab>
    <Tabs.Tab>
      ```bash copy
      $ npm install @norr/hooks
      ```
    </Tabs.Tab>
  </Tabs>

  ### Peer dependencies

  <Tabs items={['yarn', 'npm']}>
    <Tabs.Tab>
      ```bash copy
      $ yarn info @norr/hooks peerDependencies
      ```
    </Tabs.Tab>
    <Tabs.Tab>
      ```bash copy
      $ npm info @norr/hooks peerDependencies
      ```
    </Tabs.Tab>
  </Tabs>
</Steps>
