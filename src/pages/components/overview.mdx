import { Callout, Steps, Tabs } from 'nextra/components';

# Overview

<Callout type="error" emoji="🚫">
  `@norr/components` is in a early stage and is subject to change.
</Callout>

Collection of [React components](https://react.dev/reference/react/components).

## Install

<Steps>
  ### Package

  <Tabs items={['yarn', 'npm']}>
    <Tabs.Tab>
      ```bash copy
      $ yarn add @norr/components
      ```
    </Tabs.Tab>
    <Tabs.Tab>
      ```bash copy
      $ npm install @norr/components
      ```
    </Tabs.Tab>
  </Tabs>

  ### Peer dependencies

  <Tabs items={['yarn', 'npm']}>
    <Tabs.Tab>
      ```bash copy
      $ yarn info @norr/components peerDependencies
      ```
    </Tabs.Tab>
    <Tabs.Tab>
      ```bash copy
      $ npm info @norr/components peerDependencies
      ```
    </Tabs.Tab>
  </Tabs>
</Steps>
