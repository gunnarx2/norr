import { Steps, Tabs } from 'nextra/components';

# Overview

Collection of utilities.

## Install

<Steps>
  ### Package

  <Tabs items={['yarn', 'npm']}>
    <Tabs.Tab>
      ```bash copy
      $ yarn add @norr/utilities
      ```
    </Tabs.Tab>
    <Tabs.Tab>
      ```bash copy
      $ npm install @norr/utilities
      ```
    </Tabs.Tab>
  </Tabs>

  ### Peer dependencies

  <Tabs items={['yarn', 'npm']}>
    <Tabs.Tab>
      ```bash copy
      $ yarn info @norr/utilities peerDependencies
      ```
    </Tabs.Tab>
    <Tabs.Tab>
      ```bash copy
      $ npm info @norr/utilities peerDependencies
      ```
    </Tabs.Tab>
  </Tabs>
</Steps>
